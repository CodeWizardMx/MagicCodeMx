# Habilitamos la consola de H2 para poder accederla desde el navegador
spring.h2.console.enabled=true

# URL de conexi�n a la base de datos H2 en memoria. 'mem:testdb' significa que la base de datos se almacena en memoria y su nombre es 'testdb'.
spring.datasource.url=jdbc:h2:mem:testdb

# Especificamos el driver que se usar� para conectarse a la base de datos H2.
spring.datasource.driverClassName=org.h2.Driver

# Nombre de usuario para conectarse a la base de datos. En H2, el usuario por defecto es 'sa' (System Administrator).
spring.datasource.username=sa

# Contrase�a para la conexi�n a la base de datos. En este caso, la dejamos vac�a ya que H2 no requiere una contrase�a para la conexi�n predeterminada.
spring.datasource.password=

# Especificamos el dialecto de la base de datos que Hibernate utilizar�. En este caso, es el dialecto espec�fico para H2.
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Configuramos Hibernate para que actualice autom�ticamente el esquema de la base de datos basado en nuestras entidades. Esto generar� las tablas autom�ticamente si no existen.
spring.jpa.hibernate.ddl-auto=update

server.port=8081

